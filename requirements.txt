fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ORM & Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0              # SQLAlchemy 异步驱动（postgresql+asyncpg://）
psycopg2-binary>=2.9.0       # SQLAlchemy 同步驱动（建表迁移用）

# Redis
redis>=5.0.0

# Auth
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.0                # 密码哈希（jwt.py 直接 import）
python-multipart>=0.0.9      # FastAPI UploadFile / Form 运行时依赖

# File Storage (S3)
boto3>=1.34.0

# LLM & Agent
langgraph>=0.1.0
langchain-openai>=0.2.0
langchain-text-splitters>=0.3.0

# Vector (pgvector) & Numeric
pgvector>=0.3.0
numpy>=1.26.0                # 向量 L2 归一化（embedding_service / models）

# Utils
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0         # pydantic-settings 读 .env
pillow>=10.0.0

# File Parsing（本地解析，不依赖 unstructured / torch）
python-docx>=1.1.0           # .docx
python-pptx>=1.0.0           # .pptx
openpyxl>=3.1.5              # .xlsx
xlrd>=2.0.2                  # .xls（旧格式）
beautifulsoup4>=4.12.0       # .html / .htm
lxml>=5.0.0                  # BeautifulSoup 解析器后端
markdown>=3.6.0              # .md
pdfplumber>=0.11.0           # .pdf

# Safe formula evaluation（ScoreCalculationService 计算引擎，禁止任意代码执行）
simpleeval>=1.0.0

# HTML 净化（防 XSS）—— 用于 Template.description 富文本字段
nh3>=0.2.17
