fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ORM & Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0

# Schema 同步改用 SQLAlchemy `Base.metadata.create_all()`（幂等建表），不再用 alembic。
# 如未来需要 schema diff 管理，可重新引入 alembic>=1.13.0。

# Redis
redis>=5.0.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9

# File Storage (S3)
boto3>=1.34.0

# LLM & Agent
langgraph>=0.1.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0

# Vector (pgvector)
pgvector>=0.3.0

# Text Processing
tiktoken>=0.7.0

# HTTP Client
httpx>=0.27.0

# Utils
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
aiofiles>=24.0.0
pillow>=10.0.0

# Email
aiosmtplib>=3.0.0

# File Parsing
pdfplumber>=0.11.0
python-docx>=1.1.0
openpyxl>=3.1.0
mammoth>=1.6.0

# Safe formula evaluation（ScoreCalculationService 计算引擎，禁止任意代码执行）
simpleeval>=1.0.0

# HTML 净化（防 XSS）—— 用于 Template.description 富文本字段
nh3>=0.2.17
